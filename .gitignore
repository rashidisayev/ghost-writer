# SwiftPM
.build/
.swiftpm/
Package.resolved

# Assembled app bundle — produced by Scripts/build-app.sh
build/

# Compiled probe binary — build with: swiftc -O main.swift -o ax-probe
Tools/ax-probe/ax-probe

# Generated iconset staging directory (the .icns itself IS committed, so a
# fresh clone can build a bundle without running the generator first)
Resources/GhostWriter.iconset/

# macOS
.DS_Store

# Intermediate DMG background bitmaps — the combined .tiff IS committed so a
# fresh clone / CI can package without running the generator.
Resources/dmg-background.png
Resources/dmg-background@2x.png
